public class MergeSortStrivers {
    // O(N log N) worst-case. The 'merge' step is where the real work happens, not just recursion depth.
    // Auxiliary space O(N) for the temp array. Always a trade-off for stability, but cleaner than in-place tricks.
    // Recursive calls can be an elegant trap. For N > 10^5, stack depth is a legitimate concern. This ain't just LeetCode.

    
}